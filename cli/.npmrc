registry=https://registry.npmjs.org/

# GitHub Packages authentication (requires GITHUB_TOKEN env var)
@octodev-bot:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}

# Allow publishing to both registries
access=public
